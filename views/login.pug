extends layout.pug
include utils.pug

append head
    link(rel="stylesheet" href="/stylesheets/forms.css")

block content
    div.outer-form-container
        h1= title
        +validationErrorSummary(errors)
        div.form-container
            form(action="/login", method="post").form-element
                input(type="hidden", name="_csrf", value=csrfToken)

                +field("Email", "email", email, 'email')

                +field("Password", "password", null, 'password')
                div.form-buttons
                    button(type="submit").btn.signUpButton.orange Login
                    button(type="button").btn.signUpButton.cancel: a(href="/") Cancel
            .demo-user
                button(type="button").btn.signUpButton.demo: a(href="/demo-user") Demo User
